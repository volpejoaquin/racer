// Libraries
import {
  Component,
  OnInit
} from '@angular/core';

// Models

// Services

/**
 * `CircuitVariantDrawComponent` implements a component to description.
 *
 * @author Joaquin Volpe <joaquin.volpe@woopasoft.com>
 * @export
 * @class CircuitVariantDrawComponent
 * @implements {OnInit}
 */
@Component({
  selector: 'racer-circuit-variant-draw',
  templateUrl: './circuit-variant-draw.component.html'
})
export class CircuitVariantDrawComponent implements OnInit {
  // Public properties

  // Private properties

  // Public methods
  /**
   * Creates an instance of CircuitVariantDrawComponent.
   * @author Joaquin Volpe <joaquin.volpe@woopasoft.com>
   * @memberof CircuitVariantDrawComponent
   */
  constructor(
  ) {
  }

  /**
   * Implements on init method.
   *
   * @author Joaquin Volpe <joaquin.volpe@woopasoft.com>
   * @memberof CircuitVariantDrawComponent
   */
  ngOnInit() {
  }

  // Private methods
}
